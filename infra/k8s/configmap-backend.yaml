apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: web-platform
data:
  NODE_ENV: "production"
  APP_NAME: "backend-api"
  APP_VERSION: "1.0.0"
  LOG_LEVEL: "info"
  CORS_ORIGIN: "https://app.example.com"

